{
  "installCommand": "npm ci --ignore-scripts",
  "buildCommand": "npm run generate-env-install && npm run build",
  "outputDirectory": "out/renderer",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

